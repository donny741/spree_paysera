sudo: required
dist: trusty

language: ruby
rvm:
  - 2.6.3

script:
  - bundle exec rake test_app
  - bundle exec rake spec

addons:
  apt:
    packages:
    - mysql-server-5.6
    - mysql-client-core-5.6
    - mysql-client-5.6
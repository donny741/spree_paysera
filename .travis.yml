os: linux
dist: trusty

language: ruby
rvm:
  - 2.5.1

script:
  - bundle exec rake test_app
  - bundle exec rake spec

gemfile:
  - gemfiles/spree_3_7.gemfile


addons:
  apt:
    packages:
    - mysql-server-5.6
    - mysql-client-core-5.6
    - mysql-client-5.6